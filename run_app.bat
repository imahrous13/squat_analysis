@echo off
REM This script runs the Streamlit app using the correct Python environment
REM Use this instead of "streamlit run app.py" to avoid environment conflicts

echo Starting AI Fitness Coach...
python -m streamlit run app.py
